interface:
  display_name: "Replace With Skill Name"
  short_description: "One sentence describing the capability"
  default_prompt: "Use $replace-with-skill-name to handle the workflow it documents."
policy:
  allow_implicit_invocation: true
