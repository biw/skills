interface:
  display_name: "Conductor Setup"
  short_description: "Configure Conductor workspace scripts"
  default_prompt: "Use /conductor-setup to configure conductor.json and workspace scripts."
policy:
  allow_implicit_invocation: true
