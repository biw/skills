interface:
  display_name: "Review, Fix, and Address Bots"
  short_description: "Compare five persistent model reviewers"
  default_prompt: "Use $review-fix-address-bots to integrate the PR base, compare five persistent read-only Sol, Terra, and Luna reviewers, resolve findings, validate and push, then close the review-bot loop."
