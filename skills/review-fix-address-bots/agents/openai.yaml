interface:
  display_name: "Review, Fix, and Address Bots"
  short_description: "Compare reviewers and log review-bot runs"
  default_prompt: "Use $review-fix-address-bots to integrate the PR base, run and compare read-only reviewers, log findings and token usage, validate and push, then close the review-bot loop."
